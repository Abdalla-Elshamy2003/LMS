# تقرير اختبار الإنتاج — منارة

التاريخ: 12 سبتمبر 2026. البيئة: Railway production.

الرابط: https://frontend-production-a64a1.up.railway.app

## الخلاصة

**القرار: لا يُنصح باعتبار النسخة جاهزة للاستخدام الحقيقي بعد.** نجحت رحلات التعلّم والواجب والدعم الأساسية، لكن توجد مشكلة كلمة مرور إدارية تجريبية، وعطل مؤكد في قائمة امتحانات الطالب، ومشكلات نطاق الإدارة. هذا اختبار وظيفي وأمني محدود على الإنتاج وليس شهادة أمان شاملة أو اختبار اختراق كامل.

- تم رفع بيانات QA المطلوبة عبر واجهات الإنتاج الرسمية، وليس نسخ قاعدة البيانات المحلية.
- 20/20 فحصًا أوليًا: دخول المدرسين والطلاب، مكتبة الطالب، ومنع المدرس من الوصول لكورس مساحة أخرى.
- 19/20 في مجموعة الفحوص الثانية. الفشل: الطالب المسجّل يحصل على 403 عند طلب قائمة الامتحانات.
- الإجمالي 39/40 فحص API محدد. الرقم لا يشمل عدّ النقرات أو صفحات الواجهة كاختبارات مستقلة.
- جولة متصفح على 25 شاشة إدارة، ثم رحلات فعلية بحساب مدرس QA وطالب QA. فحص عرض الشاشة وحده لا يعني اختبار كل عملياتها.
- لم يتم تغيير كود الإنتاج أو إعادة نشر خدمة أثناء هذه الجولة؛ التغيير كان بيانات الاختبار ونتائجها فقط.

## بيانات الاختبار المرفوعة

كل الأسماء تبدأ بـ QA، والكورسات مجانية وموسومة بأنها للاختبار فقط. الصفحة الرئيسية الموجودة لم تتغير. الفيديو عينة CC0 تقنية لزهرة لمدة 5.055 ثانية، وليس محاضرة تعليمية.

| المادة | الأكاديمية | Tenant | الكورس | الدرس | الطلاب |
|---|---:|---:|---:|---:|---|
| الرياضيات | 2 | 4 | 53 | 9 | 32، 33 |
| الفيزياء | 3 | 5 | 54 | 10 | 34، 35 |
| الكيمياء | 4 | 6 | 55 | 11 | 36، 37 |
| اللغة العربية | 5 | 7 | 56 | 12 | 38، 39 |
| الأحياء | 6 | 8 | 57 | 13 | 40، 41 |

صفحات المدرسين: `/t/qa-20260912-teacher-1` حتى `/t/qa-20260912-teacher-5`.

بيانات الدخول محفوظة محليًا في [accounts.json](D:/LMS/.tooling/production-qa/accounts.json). يحتوي الملف كلمات مرور حساسة مولدة عشوائيًا؛ لا تشاركه أو ترفعه. المجلد `.tooling` مستبعد من Git، والملف خارج سياق رفع خدمتي backend وfrontend. لا يحتوي التقرير نفسه كلمات مرور أو JWT.

إضافات رحلة الاختبار: واجب رقم 9، تسليم رقم 121، سؤال رقم 13، امتحان QA منشور بسؤال واحد، طلب دعم رقم 1، ومرفق صورة شعار المنصة رقم 14.

## الرحلات اليدوية المنفذة

| الرحلة | النتيجة والأدلة |
|---|---|
| دخول الإدارة والخروج | نجح؛ الدخول من صفحة المدرس يضع الإدارة في نطاقه تلقائيًا |
| عرض المدرسين والكورسات | الخمسة ظهروا في شاشة المساحات والفريق، وكل كورس به طالبان |
| دخول المدرس | ظهر كورسه الواحد وطلابه الاثنان، وليس محتوى بقية QA |
| معاينة الطالب قبل الفيديو | ظهرت بيانات الدرس والوصف والمدة وعدد المرفقات. عدد عناصر video وiframe كان صفرًا قبل البدء |
| تحميل وتشغيل الفيديو | بعد البدء: readyState=4 ومدة 5.055. التشغيل من عناصر التحكم وصل إلى ended=true |
| حفظ التقدم | ظهر «أكملت هذا الدرس»، 1/1، ثم 100% بعد الخروج وإعادة الدخول |
| الواجب | إنشاء من واجهة المدرس، تسليم نص من الطالب، ظهور الإجابة عند المدرس، تصحيح 20/20، ظهور الدرجة عند الطالب |
| التقرير الدوري | توليد تقرير سبتمبر للطالب نفسه؛ تقييم HOMEWORK بدرجة 20/20 ونسبة 100% |
| الدعم | الطالب أنشأ طلبًا لمدرس QA، المدرس رد وغيّر الحالة إلى تم الحل؛ الطالب شاهد الرد والحالة بعد إعادة الدخول |
| الإشعارات | وصل إشعار الرد وإشعار تغيير حالة الطلب إلى الطالب |
| الامتحان | السؤال جُهز عبر API لأن بنك الأسئلة لا يعرض إضافة سؤال يدوية؛ إنشاء الامتحان وإضافة السؤال ونشره تمت من الواجهة. رحلة الطالب متوقفة عند قائمة الامتحانات بسبب 403؛ لم تُختبر الإجابة والتصحيح النهائي عبر الواجهة |
| المرفقات | ظهر رابط المرفق الداخلي بدون توكن في العنوان. تنزيل الملف الحقيقي عبر API نجح للطالب المسجّل ومنع للزائر والمدرس الآخر. لم يُثبت فتح تبويب مرفق مستقل عبر أداة المتصفح |

شاشات الإدارة التي تم التنقل إليها وفحص محتواها: الرئيسية، الأكاديمية، الكورسات، الجدول، الطلاب، الفريق، الدخول والخروج، قارئ الكارتات، إصدار الكارتات، التقارير، الحضور، الامتحانات، الواجبات، المدفوعات، الشهادات، لوحة الشرف، الإشعارات، الدعم، المجتمع، الحملات، قواعد التنبيه، طلبات التواصل، التدقيق، الملف الشخصي، مساحة التعلّم.

لم تظهر أخطاء Console في جولة عرض الإدارة. ظهرت أخطاء طلبات في صفحة امتحانات الطالب؛ API أكد 403. واجهة الصفحة لا تعرض رسالة فشل مفيدة وتظل ناقصة المحتوى.

## الأعطال والمخاطر

### P0 — كلمة مرور الإدارة القديمة التجريبية ما زالت تعمل

تأكد الدخول إلى حساب SUPER_ADMIN القديم بكلمة المرور التجريبية المعروفة في المشروع. تعطيل Seeder لا يغيّر كلمات مرور الحسابات الموجودة. لم يتم تغيير كلمة مرور المالك أو تعطيل حسابات قديمة أثناء اختبار الإنتاج، لتجنب قفل وصوله دون تنسيق. لا يمكن اعتماد أمان الإنتاج قبل معالجة هذه النقطة ومراجعة بقية الحسابات القديمة.

### P1 — الطالب لا يستطيع عرض امتحاناته

الطالب 32 مسجّل في الكورس 53، وامتحان QA منشور. `GET /api/exams` يعيد 403. صفحة `Exams.jsx` تستخدم المسار نفسه لجميع الأدوار، بينما `ExamController.java` يقصره على فريق التدريس والإدارة. يجب توفير قائمة طالب آمنة مع معلومات العرض اللازمة، دون كشف الإجابات الصحيحة أو توسيع صلاحيات بنك الأسئلة. تشغيل الامتحان والتسليم وإعادة المحاولة يحتاج إعادة اختبار بعد الإصلاح.

### P1 — فتح كورس أكاديمية من الإدارة العامة لا يحافظ على النطاق

الإدارة العامة ترى 57 كورسًا في `/app/courses`، بينها كورسات الأكاديميات. لكن `GET /api/courses/53` بالتوكن الإداري غير المقيّد يرجع 404؛ مع `X-Academy-Id: 2` يرجع 200. مساحة التعلّم تعرض 52 بدلًا من 57. يلزم توحيد نطاق العرض والتنقل والملخصات، وليس إزالة قيود عزل المدرسين.

### P2 — زر العودة للإدارة الرئيسية لا ينقل المستخدم

تم الضغط على الزر دون حدوث التنقل المتوقع. في `Layout.jsx:71` المتغير `location` ناتج `useLocation()`، وفي السطر 93 يُستخدم `location.href` كما لو كان `window.location`. إزالة نطاق الأكاديمية تحدث، لكن التنقل وتحديث اسم المساحة لا يحدثان تلقائيًا. الوصول إلى صفحة الأكاديمية من القائمة أتاح متابعة الاختبار.

### P2 — سجل التدقيق لا يثبت تغييرات إنشاء الحسابات

بعد إنشاء 5 أكاديميات و10 طلاب، شاشة التدقيق في الإدارة العامة عرضت «السجل فارغ». هذه ملاحظة مؤكدة عن التغطية المرئية لهذه العمليات، وليست ادعاء بأن جميع عمليات النظام غير مسجلة. الطالب مُنع من قراءة `/api/dashboard/audit` بكود 403.

### ملاحظات تحتاج إعادة إنتاج/تحسين

- في إنشاء واجب QA أُدخل 2026-09-20، لكن القائمة وAPI لم يعرضا deadline بعد الحفظ. تعيين الحقل وحفظه عبر أداة المتصفح لم يتبعه تحقق لقيمته قبل الإرسال؛ لذلك يلزم إعادة إنتاج باستخدام إدخال تاريخ فعلي وفحص الطلب لتحديد هل السبب حقل التاريخ أم التطبيق. كود الطلب والخدمة يحتويان deadline.
- «ابدأ المشاهدة» يعرض مشغلًا متوقفًا ثم يحتاج إجراء تشغيل آخر؛ لا يوجد autoplay عند فتح المعاينة.
- توجد أزرار أيقونية وحقول بلا أسماء وصول واضحة، مثل إضافة سؤال وحفظ درجة وبعض حقول النماذج.
- مسار API غير موجود تم طلبه تشخيصيًا (`/api/audit`) عاد 500 بدل 404. ليس مسار شاشة التدقيق الفعلي، ولم تتسرب تفاصيل داخلية في جسم الخطأ.

## فحوص الأمان التي نجحت

- منع وصول الطالب لزميله، لمساحة مدرس آخر، لقائمة المستخدمين، سجل التدقيق، بنك الأسئلة، وقائمة متعلمي الكورس.
- محاولة الطالب تغيير `X-Academy-Id` إلى أكاديمية أخرى رُفضت 401. تم تصحيح توقع الاختبار لقبول 401 ضمن أكواد المنع، ثم أعيد الفحص؛ لا تُعامل هذه النتيجة كتسريب.
- منع المدرسين الخمسة من قراءة كورس مساحة أخرى.
- منع الزائر والتوكن المزور من API التعلم، ومنع الزائر من الملف الحقيقي.
- رفض Origin خارجي في CORS بكود 403.
- رفض رفع SVG كملف محتوى، ومنع الطالب من رفع ملفات في materials.
- تنزيل مرفق محتوى مسجل نجح للطالب المصرح له 200، ورُفض للمدرس الآخر 403 والزائر 401.
- تأكد وجود CSP وHSTS وnosniff وReferrer-Policy=no-referrer على الصفحة العامة.

هذه الفحوص لا تثبت وجود DRM أو منع تسجيل الشاشة أو استحالة مشاركة فيديو خارجي. عينة الفيديو رابط عام عمدًا، وليست ملفًا خاصًا مدفوعًا.

## حدود الجولة وما يلزم بعد الإصلاح

لم نجر معاملات دفع حقيقية، أو إرسال بريد/SMS/WhatsApp لأشخاص حقيقيين، أو حملات جماعية، أو حذف حسابات، أو ضغط حمل على الإنتاج. لم يتم اعتماد بوابة الدفع أو الاسترداد أو webhooks أو استعادة النسخ الاحتياطية. الكاميرا وقارئ RFID والطباعة وتصدير PDF ودور ولي الأمر لم تُختبر end-to-end في هذه الجولة. إنشاء الواجب والامتحان لا يعني اختبار جميع أنواع الأسئلة والملفات والمواعيد وتداخلات الصلاحيات.

الأولوية التالية: تنسيق تأمين الحسابات القديمة، إصلاح قائمة امتحانات الطالب ونطاق الإدارة، ثم إعادة الاختبار الوظيفي والأمني للأجزاء المتأثرة واستكمال السيناريوهات غير المختبرة. لم تُطبّق إصلاحات كود ضمن طلب الاختبار الحالي.

## ملفات إعادة الفحص

- [تهيئة QA](D:/LMS/scripts/production-qa.ps1): يدعم استكمال البيانات عبر manifest محلي؛ استخدم `-VerifyOnly` لإعادة الفحوص دون تهيئة.
- [فحوص الأمان](D:/LMS/scripts/production-security-probes.ps1): يعيد استخدام مرفق الاختبار الموجود.
- النتائج الخام: `.tooling/production-qa/api-results.json` و`.tooling/production-qa/security-results.json`.

لا تعِد تشغيل التهيئة بدون manifest المحفوظ، ولا تنشر ملفات كلمات المرور مع أي خدمة.
