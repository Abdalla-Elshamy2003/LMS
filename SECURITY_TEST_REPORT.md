# تقرير الاختبار والأمان — منصة منارة

## تحديث النشر على Railway — 12 سبتمبر 2026

تم نشر النسخة على مشروع `manarah-lms` في بيئة `production` بحساب `abdallaelshamu781@gmail.com`.

- رابط الموقع: https://frontend-production-a64a1.up.railway.app
- Backend deployment: `c3a84979-577a-47cd-a719-c43d542303c1` — `SUCCESS`.
- Frontend deployment: `8d236d0e-c13f-4cac-a5bb-dc65be77c8b7` — `SUCCESS`.
- الصفحة الرئيسية و`/healthz`: HTTP 200. واجهة `/api/public/landing` استجابت بنجاح.
- ملفات JavaScript المشار إليها في HTML المنشور تطابق ملفات البناء المحلي.
- تم التحقق من CSP وHSTS وnosniff وReferrer-Policy وPermissions-Policy عبر HTTPS.
- `/api/learning` بدون تسجيل، ورابط ملف بتوكن في Query String: HTTP 401.
- تم ضبط Secure cookies وCORS على دومين الموقع، وتعطيل Demo Seeder وAPI docs في إعدادات الإنتاج.
- قاعدة البيانات والملفات على Volume الحالي `/app/data` محفوظة. لم تُنسخ قاعدة البيانات المحلية أو بيانات المدرسين التجريبية إلى الإنتاج؛ أعداد البيانات الواردة أدناه تخص الفحص المحلي.
 
هذا التحديث يثبت النشر والفحوص العامة المذكورة فقط؛ لا يمثل اختبار اختراق مستقل أو إعادة اختبار كل رحلة مستخدم على الإنتاج.

**تاريخ التنفيذ:** 12 سبتمبر 2026  
**النطاق:** نسخة التطوير المحلية الكاملة عبر Docker (واجهة + API + قاعدة البيانات المحلية)  
**النتيجة:** ناجح ضمن النطاق الموضح أدناه

## ملخص تنفيذي

- تم تشغيل الواجهة والباك إند داخل Docker، والحاويتان في حالة `healthy`.
- نجحت 15 حالة تكامل وانحدار: 4 لمسار التعلم، 1 لمساحة المدرس، و10 للأمان والصلاحيات.
- نجح بناء الواجهة الإنتاجي، وأظهر `npm audit` عدد 0 ثغرات معروفة في 362 حزمة.
- تم فحص 131 اعتماد Runtime للباك إند عبر OSV، وكانت النتيجة 0 ثغرات معروفة.
- البيانات الحية تحتوي 6 مدرسين حفاظًا على المدرس الموجود سابقًا. الخمسة المطلوبون لديهم كورسات وطلاب وفيديو معاينة؛ وكل المدرسين الستة لديهم كورس وطلاب.
- تم اختبار رحلة الطالب بصريًا: شاشة الملخص ظهرت قبل الفيديو، وكان عدد عناصر الفيديو في الصفحة 0 قبل الضغط على «ابدأ المشاهدة» ثم 1 بعده.

## تغطية الأمان المنفذة

### المصادقة والجلسات

- رفض التوكن في Query String لمنع تسربه في السجل، التاريخ، و`Referer`.
- ملفات الدروس الداخلية تستخدم Cookie محدودة للمسار `/api/files` مع `HttpOnly` و`SameSite=Strict` بدل وضع التوكن في الرابط.
- إعادة تحميل المستخدم من قاعدة البيانات في كل طلب محمي ورفض الحساب غير النشط فورًا.
- ربط صلاحية JWT ببصمة كلمة المرور، ولذلك يؤدي تغيير كلمة المرور إلى إبطال التوكنات القديمة.
- فرض JWT secret قوي، وتقليل عمر Access Token، وإضافة Rate Limit لمحاولات الدخول مع رسالة موحدة تمنع كشف وجود الحساب.
- سياسة كلمة مرور من 10 إلى 72 محرفًا، وتتطلب حرفًا ورقمًا.

### الصلاحيات وعزل البيانات

- تأمين ملكية الكورس: المدرس لا يعدّل كورس مدرس آخر.
- منع الطالب من قراءة قوائم طلاب الكورس أو رفع ملفات المحتوى.
- تأمين محاولات الامتحان بحيث لا يستطيع طالب تسليم محاولة طالب آخر.
- التحقق من التسجيل، النشر، والتوقيت قبل بدء الامتحان.
- تأمين الواجبات، الدرجات، المواد، والتنزيلات بحسب الدور والـtenant وملكية المورد.
- منع تنزيل الملفات اليتيمة أو الوصول عبر مسار متلاعب به.

### الرفع والتنزيل

- Allow-list للمجلدات، الامتدادات، وأنواع الملفات مع حدود للحجم.
- التحقق من ارتباط الملف الفعلي بدرس أو واجب أو تسليم مصرح به قبل تنزيله.
- رؤوس `no-store` و`nosniff` و`Content-Disposition` للاستجابات الحساسة.

### الواجهة والـHTTP

- رفض CORS لمصدر غير موثوق، مع السماح فقط للمصادر المحددة صراحة.
- تفعيل CSP و`X-Content-Type-Options` و`Referrer-Policy` و`Permissions-Policy` و`Cross-Origin-Opener-Policy`.
- Swagger/OpenAPI مغلق افتراضيًا في الإنتاج، ومقيد للـSuper Admin عند تفعيله.
- منع HTTP Basic وForm Login الافتراضيين، وإرجاع JSON موحد لحالات 401.
- روابط YouTube تستخدم نطاق privacy-enhanced و`no-referrer`، والفيديو المباشر يستخدم `controlsList="nodownload"` كحاجز واجهة إضافي.

### Webhooks والسجلات

- WhatsApp webhook يتحقق من توقيع Meta HMAC-SHA256 على الـraw body ويحتاج Verify Token مضبوطًا.
- لم تعد السجلات تطبع نص الرسالة أو رقم الهاتف كاملًا أو استجابة بوابة الدفع كاملة.
- رسائل الأخطاء الداخلية تُسجل في الخادم، بينما العميل يتلقى رسالة عامة لا تكشف Stack Trace.

### الاعتمادات

- تحديث Spring Boot إلى 3.5.16 وJJWT إلى 0.13.0 وspringdoc إلى 2.8.17 وSQLite JDBC إلى 3.53.4.0.
- تثبيت إصدارات Jackson وLog4j وCommons Lang المصححة.
- استبدال Tomcat المضمن بـJetty لإزالة الثغرات التي كانت باقية في نسخة Tomcat المتاحة وقت الاختبار.

## نتائج HTTP الحية

| الفحص | النتيجة |
|---|---:|
| مسار التعلم بدون تسجيل | 401 |
| محاولة تمرير توكن داخل رابط ملف | 401 |
| Swagger بدون تسجيل | 401 |
| CORS من مصدر غير موثوق | 403، بدون `Access-Control-Allow-Origin` |
| تسجيل الدخول | 200 |
| Cookie الملفات | HttpOnly + SameSite=Strict + Path مقيد |
| محاولة طالب دخول مسار إدارة المدرسين | 403 |
| الصفحة الرئيسية | 200 |
| رؤوس الأمان الأساسية | 5/5 |

## بيانات العرض الحية

| المدرس | الكورسات | الطلاب | فيديو المعاينة |
|---|---:|---:|---:|
| أ. محمد الجندي | 1 | 21 | 1 |
| أ. سارة عبد الله | 1 | 12 | 1 |
| أ. أحمد رشدي | 1 | 21 | 1 |
| أ. منى صالح | 1 | 17 | 1 |
| أ. ريم مصطفى | 1 | 10 | 1 |

يوجد أيضًا مدرس سادس محفوظ من البيانات السابقة وله كورس وطالب. فيديوهات العرض المضافة للبيانات القديمة **عينات محلية/تجريبية موضحة بهذا الوصف** وليست محاضرات أكاديمية نهائية؛ يجب أن يستبدلها كل مدرس بتسجيله الحقيقي.

## حدود النتيجة ومتطلبات الإنتاج

هذا فحص تطبيق وتكامل قوي للنسخة المحلية، لكنه ليس شهادة أن النظام «غير قابل للاختراق» ولا يغني عن Penetration Test مستقل بعد النشر. قبل الإنتاج يجب:

1. تعطيل Demo Seeder وعدم استخدام أي كلمة مرور تجريبية.
2. استخدام HTTPS فقط وضبط Cookie `Secure=true` وتحديد CORS على الدومين الحقيقي فقط.
3. إبقاء API docs مغلقة، وتدوير JWT/WhatsApp/payment secrets داخل Secret Manager.
4. نقل Rate Limiting إلى Redis أو API Gateway/WAF عند تشغيل أكثر من نسخة Backend.
5. اختبار إعدادات الشبكة، TLS، النسخ الاحتياطي، صلاحيات قاعدة البيانات والتخزين على بيئة الاستضافة الفعلية.
6. إجراء DAST/Penetration Test على الدومين المنشور، لأن البيئة المحلية لا تثبت إعدادات Cloud أو WAF أو CDN.
7. تشغيل فحص الاعتمادات تلقائيًا في CI في كل Pull Request.
8. استبدال فيديوهات الـplaceholder بتسجيلات المدرسين ويفضل تخزين الفيديوهات الخاصة خلف CDN بتوقيعات قصيرة العمر أو DRM عند الحاجة. منع زر التنزيل لا يمنع تسجيل الشاشة أو استخراج بث عام.

## ملاحظة أداء

البناء الإنتاجي ناجح، لكن Vite ما زال يحذر من حزمتين JavaScript أكبر من 900KB. هذا ليس فشلًا أمنيًا، لكنه بند تحسين أداء لاحق عبر lazy loading وتقسيم حزم PDF/Three.js.
